# Dependencies
node_modules/

# Build output (regenerated by `npm run build`)
_site/

# Eleventy / eleventy-img cache
.cache/

# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Env / secrets (none committed yet — real keys go here once issued, never in site.json)
.env
.env.local
